
// MOCK FIREBASE CONFIG
// We are using mock data services (AuthContext / db.ts) so we don't need real Firebase init.
// This prevents crashes when keys are missing.

export const auth = {};
export const db = {};
